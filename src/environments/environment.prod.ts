// Swapped in for environment.ts by angular.json's `fileReplacements` when building
// with the `production` configuration (`ng build` defaults to it).
export const environment = {
    production: true,
    // TODO: replace with the real production API origin once the backend is deployed.
    apiBaseUrl: 'https://magen-backend-production.up.railway.app'
};
