// Local development defaults. Never imported into a production build — swapped out
// via angular.json's `fileReplacements` for the `production` build configuration.
export const environment = {
    production: false,
    apiBaseUrl: 'http://localhost:3000'
};
